export interface JobRole {
  title: string;
  industry: string;
  salaryRange: string;
  avgSalary: number;
  aiExcelUsage: string;
  keySkills: string[];
}

export interface MarketInsight {
  trend: string;
  description: string;
}
